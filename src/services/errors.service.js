export const errorMessage = {
  invalidType: 'One or more fields are invalid type.',
  requiredFields: 'One or more fields are required.',
  duplicatedKey: 'One item already exists with that key.',
  exceedsMaxValue: 'One or more fields exceeds maximum allowed value.',
  exceedsMinValue: 'One or more fields exceeds minimum allowed value.',
  internalServerError: 'Internal server error.',
  invalidCredentials: 'Invalid credentials.'
};
